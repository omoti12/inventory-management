# 在庫管理システム（モック）

商品の入庫、在庫確認、出荷、出荷履歴の確認を、ブラウザだけで試せるサンプルアプリです。
データはこのパソコンのブラウザ内にだけ保存され、他の人やサーバーには送信されません。

## まずは起動する

特別な準備やインストールは不要です。ターミナルで、このリポジトリのフォルダを開いてから次を実行してください。

```sh
./scripts/start.sh
```

表示された案内に従い、ブラウザで [http://localhost:8000](http://localhost:8000) を開きます。
画面が表示されたら起動完了です。終了するときは、スクリプトを実行したターミナルで `Ctrl + C` を押してください。

### うまく起動できないとき

- `Permission denied` と表示される場合は、最初の一度だけ `chmod +x scripts/start.sh` を実行してください。
- `Python 3 が見つかりません` と表示される場合は、Python 3 をインストールしてから再実行してください。
- 8000番ポートを別のアプリが使っている場合は、別の番号を指定できます。例: `./scripts/start.sh 8080`。その場合は [http://localhost:8080](http://localhost:8080) を開きます。

製造番号のバーコード読み取りでカメラを使うには、ブラウザのカメラ利用を許可してください。カメラを使えない場合でも、製造番号は手入力できます。

## できること

| 画面 | 内容 |
| --- | --- |
| 在庫一覧 | 型名・図番・製造番号・案件番号・入荷月で検索。「明細」と「型名まとめ（在庫数の集計）」を切替 |
| 入庫 | 商品管理で登録済みの型名をプルダウンで選択（寸法・図番は自動表示）。製造番号・入荷月・案件番号を入力して在庫に登録。製造番号はカメラでのバーコード読み取りにも対応。コピー登録と連続登録モードで連番の商品に対応 |
| 出荷 | 在庫から選んだ商品を出荷。出荷した人・出荷先・案件番号が揃うまで出荷ボタンは押せない（宛先は任意） |
| 出荷履歴 | 出荷実績の一覧。出荷済みの商品はキャンセルして在庫に戻せる（履歴は残る） |
| 商品管理 | 商品マスタ（型名・寸法・図番）の登録・編集・削除。在庫・出荷履歴で使用中の商品は削除できない |

ログイン機能はありません。出荷時には担当者名を入力して記録します。

## データについて

- データはブラウザの **localStorage** にのみ保存される（サーバー・DBなし）。
- 端末・ブラウザごとに独立していて、他の人とは共有されない。
- 初回起動時にはデモデータが入ります。画面下部の「デモデータを初期状態に戻す」でいつでも初期化できます。

> 注意: ブラウザのサイトデータを削除すると、このアプリに登録したデータも消えます。

## このモックで扱わないこと

- **型名・寸法・図番のQRコード・写真読み取り** — 設計・実装していない。商品マスタの登録は手入力で行う。
  （製造番号のバーコード読み取りは実装済み。読み取った値は確認・修正してから登録する。）
- ログイン・権限管理 — 要件どおり設けていない。
- サーバー / DB / API — モックのため localStorage のみ。

## 詳しい資料

| ドキュメント | 役割 |
| --- | --- |
| このファイル | 動かし方・できること・スコープ |
| [docs/requirements.md](docs/requirements.md) | 何を作るか（機能要件・入力項目・業務フロー） |
| [docs/design.md](docs/design.md) | どう作ったか（ファイル構成・データモデル・本実装への申し送り） |

## フォルダ構成

```
AGENTS.md     Codex 向けの作業ガイド
CLAUDE.md     Claude 向けの作業ガイド
README.md     利用方法と機能概要
docs/         要件定義・設計メモ
scripts/      起動など、繰り返し使う操作用のスクリプト
  start.sh    アプリをローカルで起動するスクリプト
src/          実行可能なフロントエンド一式
  index.html  画面（5タブ）
  css/        スタイル
  js/         データモデル（store.js）と画面ごとのモジュール
```

コードを編集する方は、ファイルごとの役割を [docs/design.md](docs/design.md) の「ファイル構成」で確認してください。
